# Docker

## Goal

Docker 规范的目标是让镜像构建稳定、运行一致、交付可复现。

## Rule

- `Dockerfile` 保持清晰，减少无意义层
- 尽量控制镜像体积，不把构建垃圾带进运行镜像
- 统一基础镜像来源和版本策略
- 环境差异通过配置注入，不写死在镜像里
- 不把密钥、token、私钥打进镜像

## Good Bias

- 区分 build 阶段和 runtime 阶段
- 启动命令、健康检查、暴露端口要明确
- 镜像标签策略要可追踪，例如版本号、提交号、环境语义
